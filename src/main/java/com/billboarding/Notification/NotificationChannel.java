package com.billboarding.Notification;

/**
 * Defines which channel to use for sending notifications.
 */
public enum NotificationChannel {
    EMAIL,   // Only email
    SMS,     // Only SMS
    BOTH     // Email + SMS
}
